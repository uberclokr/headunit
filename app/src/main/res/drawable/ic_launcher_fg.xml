<?xml version="1.0" encoding="utf-8"?>
<!-- Adaptive-icon foreground: the vehicle rear graphic (the same Xterra art the
     tilt gauges use), centred within the 108dp canvas' safe zone over the dark
     ground. Raster bitmap inset so it sits inside the launcher mask. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:drawable="@drawable/ic_truck_rear"
        android:gravity="center"
        android:width="66dp"
        android:height="66dp" />
</layer-list>
